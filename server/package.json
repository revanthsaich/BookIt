{
  "name": "bookit-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node ./server.js",
    "build": "echo \"no build (JS-only server)\"",
    "vercel-build": "echo \"no build (JS-only server)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "serverless-http": "^3.0.0"
  }
}
