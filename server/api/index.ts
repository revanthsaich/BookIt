import handler from "../vercel.js";

// This file is the Vercel function entrypoint (server/api/index.ts).
// When deployed, Vercel will import this module as a serverless function.
// Export the serverless handler as the default export.
export default handler;
